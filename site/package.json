{
  "name": "tx9-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler types --strict-vars=false && tsc --noEmit",
    "test": "node --import ./tests/text-modules.mjs --test tests/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "wrangler": "^4.48.0"
  }
}
